var Header = createReactClass({
    render: function () {
        return (
            <header id="header-container">
                <h1>{STRINGS.HEADER_WELCOME}</h1>
            </header>
        )
    }
});
